package com.unionpay.services.repository;

import com.unionpay.services.model.Server0pinionFeedback;
import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.data.jpa.repository.JpaSpecificationExecutor;
import org.springframework.stereotype.Repository;

@Repository
public interface Server0pinionFeedbackRepository extends JpaRepository<Server0pinionFeedback,Integer>, JpaSpecificationExecutor<Server0pinionFeedback> {
}
